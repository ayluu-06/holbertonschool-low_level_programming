#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - alphabet
 * Return: 0
 */

int main(void)
{
	char variable, cap;

	for (variable = 'a'; variable <= 'z'; variable++)
		putchar(variable);
	for (cap = 'A'; cap <= 'Z'; cap++)
		putchar(cap);

{
		putchar('\n');
}
	return (0);
	}
