#include "main.h"

/**
 * factorial - fun
 * @n: var
 * Return: lajkcdn
 */

int factorial(int n)
{
	if (n == 0)
	{
		return (1);
	}
	else if (n < 0)
	{
		return (-1);
	}
	else
	{
		return (n * factorial(n - 1));
	}
}
